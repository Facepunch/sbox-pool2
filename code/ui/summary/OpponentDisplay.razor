@using Sandbox;
@using Sandbox.UI;
@using Sandbox.UI.Construct;
@using System;

@namespace Facepunch.Pool
@attribute [StyleSheet( "/ui/summary/WinSummary.scss" )]
@inherits Panel

<root>
	<label class="opponent-text">
		@if (Outcome == EloOutcome.Win)
		{
			@:You beat
		}
		else
		{
			@:You lost to
		}
	</label>

	<div class="opponent-background">
		<img src="avatar:@Player.SteamId" class="opponent-avatar" />
		<label class="opponent-name">@Player.SteamName</label>
		<RankIcon Rank=@Player.Elo.GetRank() Level=@Player.Elo.GetLevel()></RankIcon>
	</div>
</root>

@code
{
	public EloOutcome Outcome { get; set; }
	public PoolPlayer Player { get; set; }
}
